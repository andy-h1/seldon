import React from 'react';
import { BarChartCards } from '.';

export default {
  title: 'BarChartCards',
  component: BarChartCards,
};

export const Template = (args) => <BarChartCards {...args} />;
