import React from 'react';
import { HeaderDashboard } from '.';

export default {
  title: 'HeaderDashboard',
  component: HeaderDashboard,
};

export const Template = (args) => <HeaderDashboard {...args} />;
