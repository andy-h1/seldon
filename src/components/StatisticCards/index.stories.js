import React from 'react';
import { StatisticCards } from '.';

export default {
  title: 'StatisticCards',
  component: StatisticCards,
};

export const Template = (args) => <StatisticCards {...args} />;
