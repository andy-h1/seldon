import React from 'react';
import { TableAddition } from '.';

export default {
  title: 'TableAddition',
  component: TableAddition,
};

export const Template = (args) => <TableAddition {...args} />;
