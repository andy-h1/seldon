import React from 'react';
import { UsageStatistics } from '.';

export default {
  title: 'UsageStatistics',
  component: UsageStatistics,
};

export const Template = (args) => <UsageStatistics {...args} />;
